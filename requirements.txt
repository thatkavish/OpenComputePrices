# Core collectors use only the Python standard library (no external deps).
# Playwright is required only for browser-based scrapers (--browser flag).
#
# stdlib modules used: csv, json, logging, urllib.request,
#                      argparse, os, sys, time, datetime, re

# Required for browser-based scrapers (optional if only running API collectors)
playwright>=1.40.0
